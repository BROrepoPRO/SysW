# Объединённый план невыполненных работ — SysW v1.0.3

> **Целевая версия:** v1.0.3
> **Назначение:** единый план всех открытых работ, перенесённых из закрытых/частично выполненных планов (`fix_architecture_matlib_plan.md`, `fix_consistency_discrepancies_plan.md`, `fix_deploy_path_plan.md`, `fix_tc23_compile_error_plan.md`, `integration_1.0.1.md`, `update_docs_plan.md`) и этапа актуализации документации.
> **Правила:** строго соблюдать `.ycarules`. Изменения VBA/CI/конфигов — только после согласования (правила `[U1]`, `[Z5]`, `[E3]`).

---

## Сводка

Данный план фиксирует **будущие работы**, которые будут выполняться отдельными этапами после утверждения. Каждый раздел соответствует самостоятельной области: логгер, окружение, рефакторинг, CI, конфиги, документация, завершение.

Фактические метрики на момент создания плана: `src/` = **13 `.bas` + 2 класса (`PartIdentity`, `WorkIdentity`) + 1 лист (`Лист2_main`) = 16 VBA-файлов**.

---

## A. Логгер (ВЫСОКИЙ РИСК — изменение VBA-кода)

**Затрагиваемые файлы:** `src/modules/Mod_Logger.bas`, `src/modules/Mod_Utils.bas`, 9 точек вызова.

Задачи:
1. Добавить `Enum LogLevel` (значения, например: `Error`, `Warning`, `Info`, `Debug`).
2. Добавить параметр уровня в сигнатуру `WriteLog` в `Mod_Logger.bas`.
3. Формат записи: префикс `[УРОВЕНЬ]` в начале строки лога.
4. Заменить **9 вызовов** `Mod_Utils.WriteLog(msg)` на прямые `Mod_Logger.WriteLog(модуль, уровень, msg)`.
5. После замены всех вызовов — удалить/ослабить обёртку `Mod_Utils.WriteLog` (обратная совместимость).

**Требования:**
- Отдельное согласование с пользователем перед изменением сигнатуры публичной функции.
- Прогон `run_tests.py` и проверка в Excel (логи пишутся с корректным уровнем и префиксом).

---

## B. Окружение

**Затрагиваемые файлы:** корень проекта (новый `requirements.txt`), `.venv`.

Задачи:
1. Создать `requirements.txt` — перечень зависимостей Python-скриптов (`scripts/check_docs.py`, `scripts/run_tests.py`, `scripts/export_vba.py`, `scripts/impVBA.py`).
2. Пересоздать `.venv` (активация через `.venv\Scripts\Activate.ps1`, PowerShell 7 — правило `[S9]`).

---

## C. Рефакторинг (Code/Debug)

### C.1 Рассинхрон имени листа (ВЫСОКИЙ РИСК — изменение VBA)
- Исправить рассинхрон имени `"Sheet1_main"` → `"Лист2_main"` в `src/sheets/Лист2_main.cls:45`.

### C.2 Перенос объявлений Dim (ВЫСОКИЙ РИСК — изменение VBA)
- Перенести объявления `Dim` в начало процедур в `src/modules/Mod_FullTestRunner.bas`.
- **Требуется:** отдельное согласование + прогон `run_tests.py` / Excel.

### C.3 Проверка данных UAZz4
- Проверка данных листа `UAZz4` (МОЖЕТ ТРЕБОВАТЬ ручного запуска Excel — согласовать с пользователем; вероятно в режиме Debug).

---

## D. CI

**Затрагиваемый файл:** `.github/workflows/vba-check.yml`.

Задачи:
1. Исправить шаг `Check CHANGELOG updated` под фактический путь `docs/CHANGELOG.md` (корневого `CHANGELOG.md` нет).
2. Исправить шаг `Check .gitignore consistency` — корневого `.gitignore` нет; привести проверку к фактической структуре (учесть отсутствие файла либо заменить на ссылку на `docs/`).

---

## E. Конфиги

### E.1 `.sourcecraft`
- Привести critical-пути к фактическим:
  - `table.md` → `docs/table.md`
  - `CHANGELOG.md` → `docs/CHANGELOG.md`
  - `plans/ROADMAP.md` → `docs/ROADMAP.md`

### E.2 `.ycarules`
- Убрать упоминание удалённого `scripts/sync_fix.ps1` (см. `[S4]`).
- Привести пути документации в разделе 6 `[S1]` в соответствие фактической структуре (`docs/`).

---

## F. Документация

Детальное описание правок — см. Этап 3 плана `update_plans_and_docs_plan.md` (актуализация README, `docs/ARCHITECTURE.md`, `docs/DEVELOPER.md`, `docs/CHANGELOG.md`, `docs/ROADMAP.md`, `docs/git-workflow.md`, `docs/sourcecraft-guide.md`, `docs/table.md`).

Основные направления:
- Актуализация описания компонентов до фактических 16 VBA-файлов и 1 листа (`Лист2_main`).
- Удаление упоминаний `.bak`-листов (`Sheet_work.cls.bak`, `Sheet_z4.cls.bak`).
- Замена устаревших абсолютных путей `L:\...` на относительные/актуальные.
- Синхронизация упоминаний `CHANGELOG.md`/`.gitignore` с фактическими путями (`docs/CHANGELOG.md`, отсутствие корневого `.gitignore`).
- Поднятие версии до v1.0.3 и актуализация таблицы истории версий.

---

## G. Завершение

Задачи:
1. Обновить `docs/CHANGELOG.md` — запись `v1.0.3`.
2. Прогнать `scripts/check_docs.py --check` и CI-шаг `docs-check.yml` — без ошибок.
3. Перенести закрытые планы в `plans/_archive/` (по завершении соответствующих работ).

---

## Пометки о высоком риске

| Пункт | Риск | Мера |
|-------|------|------|
| Раздел A (логгер) | Высокий — смена сигнатуры `WriteLog`, 9 вызовов | Отдельное согласование + тест в Excel / `run_tests.py` |
| Раздел C.2 (перенос `Dim`) | Высокий — структурные правки `Mod_FullTestRunner.bas` | Отдельное согласование + прогон тестов |
| Раздел C.3 (проверка `UAZz4`) | Средний — возможен ручной запуск Excel | Привлечь пользователя / Debug |

---

## Порядок выполнения

Разделы A–G выполняются последовательно, каждый — после явного согласования с пользователем там, где это требуется (A, C.2, C.3). Раздел F (документация) и поднятие версии могут выполняться параллельно с подготовкой остальных разделов.