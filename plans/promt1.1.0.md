# Задание для SourceCraft `plans/promt1.1.0.md`

**!!!ВАЖНО: `.ycarules` имеет высший приоритет!!!**

## Цель

### Версия

Версия проекта SysW — **1.1.0**

## Предшествующий статус (1.0.17 → 1.1.0)

### Выполненные и закоммиченные задачи 1.0.17

- **Задача 1** — создание папки временных скриптов (коммит `f9c904d`).
- **Задача 2** — правило «перед изменением ключевых файлов Excel — всегда делать копию» (коммит `ceb0b0e`).
- **Задача 3** — исправление вывода PowerShell в чате SourceCraft (коммит `9313139`).
- **Задача 5** — схема резервирования/обновления ключевых файлов + библиотека запретов редактирования (коммит `2628657`).
- **Задача 7** — правило «единый запрос на изменение критических файлов подзадачи» (коммит `62ac536`).
- **Задача 8** — интеграция gruntfuggly.todo-tree@0.0.226 (коммит `bd0a403`).

> Указанные задачи **не дублируются** как новые, их результаты считаются действующими.

### Частично выполненная задача 1.0.17

- **Задача 6** (формат шаблонов) — выполнена частично: скрипт `scripts/apply_sheet_format.py` создан, формат применён к шаблонам `base/templates/*`. Рабочие книги получат формат при сборке.

### Что переносится из 1.0.17 (незавершённое)

- **Задача 4** — модификация логгера (2 лога) + переработка списка тестов.
- Незавершённые этапы «РАБОЧЕЙ СХЕМЫ» 1.0.17: Проверка (Debug), Обновление документации (DEVELOPER, ARCHITECTURE, ROADMAP, table, CHANGELOG, при необходимости README и sourcecraft-guide), Очистка системы, Коммит/пуш в `dev` и слияние `dev → main`.

## Задачи

### Задача 1 (из 1.0.17, оригинал №4)

1. Модифицировать логгер так, чтобы выводилось **2 лога:**

- общие системные;
- расширенные от тестов (включая ошибки VBA).

2. Переработать список тестов исходя из актуальной версии системы

После этой задачи выполнить коммит.

### Задача 2 (из 1.0.17)

Завершить незакрытые этапы «РАБОЧЕЙ СХЕМЫ» 1.0.17:

- Проверка (Debug).
- Обновление документации: `docs/DEVELOPER.md`, `docs/ARCHITECTURE.md`, `docs/ROADMAP.md`, `docs/table.md`, `CHANGELOG.md`, при необходимости `README.md` и `docs/sourcecraft-guide.md`.
- Очистка системы.
- Коммит/пуш в ветку `dev` и слияние `dev → main` (по согласованию с пользователем).

### Задача 3 (из 1.0.18, оригинал №1)

Модернизировать версиозность проекта - если есть изменения  внутри одного промта  с изменением todo - GHANGELOG - commits тогда версия проекта становится Х.Х.Х.Х. Пример: 1.0.18.1

### Задача 4 (из 1.0.18, оригинал №2)

Синхронизировать c взаимными ссылками и одинаковой версией (проекта) todo - GHANGELOG - git commits

После этой задачи выполнить коммит.

### Задача 5 (из 1.0.18, оригинал №3)

ВНЕСТИ ВО ВСЕ ПРАВИЛА И ИНСТРУКЦИИ СИСТЕМЫ!!!: "Если путь к объекту заблокирован, приоритет - запрос к пользователю на снятие блокировки"

### Задача 6 (из 1.0.18, оригинал №4)

Добавить в это правило или создать новое:

- ключевые файлы системы (скрипты, модули)

- выполнение последовательности  команд python, ps и др.

**`[U4]` ЕДИНЫЙ ЗАПРОС НА КРИТИЧЕСКИЕ ФАЙЛЫ ПОДЗАДАЧИ**
  Если выполнение подзадачи требует изменения нескольких критических файлов (см. `[E3]`) –
  запрос на изменение следует провести **один раз для всех файлов подзадачи**, одной точкой
  подтверждения (а не поочерёдно на каждый файл). Запрос должен перечислять все целевые файлы
  и планируемые правки одним блоком. Пример: групповая комбинация git-команд (add/commit/push),
  покрывающая все файлы подзадачи за один вызов.

Запустить в системе.

После этой задачи выполнить коммит и пуш.

### Задача 7 (из 1.0.18, оригинал №5)

**Architect** совместно с юзером разработать цветовую схему листов проекта, формат ячеек и другие дизайнерские аспекты объектов для придания лучшей общей восприимчивости информации и картинки с экрана юзером. **Architect** провести анализ, предложить варианты

Вынести в отдельную глобальную отложенную задачу

После этой задачи выполнить коммит/пуш.

### Задача 8 (из 1.0.18, оригинал №6)

Провести анализ актуальности следующей задачи: "Создать и поместить в todo отложенную задачу по параллельной разработке/миграции системы на убунту хх.х.х."

Вынести в отдельную глобальную отложенную задачу

После этой задачи возможен коммит (при необходимости).

### Задача 9 (из 1.0.18, оригинал №7)

Создать скрипт очистки системы, чтобы юзер мог вручную запускать очистку системы от временных файлов, старых логов, файлов, бэкапов, кэша через терминал vscode.

После этой задачи возможен коммит (при необходимости).

### Задача 10 (из 1.0.18, оригинал №8)

В схеме резервирования реализовать указание кол-ва бэкапов

После этой задачи возможен коммит (при необходимости).

### Задача 11 (из 1.0.18, оригинал №9)

Добавлять с выполнением длительных процессов:

pwsh -NoProfile -Command "New-Item -ItemType Directory -Force -Path 'logs' | Out-Null; $log='logs/monitor_task6.log'; Add-Content -Path $log -Value ('--- Старт мониторинга '+ (Get-Date -Format 'HH:mm:ss') + ' ---'); for($i=1;$i -le 40;$i++){ $excel=@(Get-Process EXCEL -ErrorAction SilentlyContinue).Count; $py=@(Get-Process python -ErrorAction SilentlyContinue).Count; $line=('[{0}] опрос {1}: EXCEL={2} python={3}' -f (Get-Date -Format 'HH:mm:ss'),$i,$excel,$py); Write-Host $line; Add-Content -Path $log -Value $line; if($excel -eq 0 -and $py -eq 0){ $msg=('ЗАВЕРШЕНО: '+ (Get-Date -Format 'HH:mm:ss')); Write-Host $msg; Add-Content -Path $log -Value $msg; break }; Start-Sleep -Seconds 180 }"

### Задача 12 (из 1.0.18, оригинал №10)

ВНИМАНИЕ!!! Внести изменение в глобальное поведение агентов:

"Вместо создания плана на промт - необходимо регистрация задачи в todo."

Запустить в системе.

После этой задачи выполнить коммит/пуш.

### Задача 13 (из 1.0.18, оригинал №11)

интегрировать docs\reestr.md в общую документацию системы с удалением из системы

при необходимости вынести в отдельную глобальную отложенную задачу

После этой задачи выполнить коммит/пуш.

### Задача 14 (из 1.0.18, оригинал №12)

Прокомментировать пользователю возможность насройки вывода лога процессов в терминал вскоде без предоставления контекста АИ.

### Задача 15 (из 1.0.18, оригинал №13)

- все задачи внести в todo
- Задача 10 выполнить  - первой, а Задача 5 выполнить  - второй (в терминах оригинальной нумерации 1.0.18: оригинал №10 = Задача 12 текущего промта, оригинал №5 = Задача 7 текущего промта)
- остальные задачи промта можно выполнять **в любом удобном для написания кода порядке**, после — по «РАБОЧЕЙ СХЕМЕ».

## РАБОЧАЯ СХЕМА

### 1. Анализ задачи (роль: **Ask**)

- анализ для планирования

### 2. Планирование (роль: **Architect**)

- на основе анализа Ask спроектировать план всех обновлений в систему по текущему промту, при необходимости - декомпозировать задачи на более мелкие или выносить в отдельные.

### 3. Исполнение (роль: **Code**)

- при необходимости передавать исправление ошибок **Debug**

### 4. Проверка (роль: **Debug**)

- при возможности - использовать прямой диагностический прогон  с автозакрытием диалогов

### 5. Координация (роль: **Orchestrator**)

- контролировать работу с todo

### 6. Обновление документации

Обновить обязательно:

- `docs/DEVELOPER.md`
- `docs/ARCHITECTURE.md`
- `docs/ROADMAP.md`
- `docs/table.md`
- `CHANGELOG.md`
- `README.md`
- `docs/sourcecraft-guide.md`

При важности вносить промежуточные изменения

### 7. Очистка системы

- Удалить временные файлы, если они появились.
- Проверить, что не осталось устаревших упоминаний в коде и документации.

### 8. Коммит, пуш, слияние

- Закоммитить изменения в ветку `dev`.
- Отправить в `origin/dev`.
- Запросить у пользователя подтверждение на слияние `dev → main`.
- После подтверждения выполнить слияние.
- Вернуться на ветку `dev`.

## Критерии приёмки

### Проверка решений

- Все решения пользователя из Задач применены (или явно согласованы).

### Документация

- Документация актуализирована.

### Шаблоны

- Шаблоны `base\templates\` и модельные книги настроены согласно требованиям.

### Чистота

- Проект очищен.

### Git

- Коммит и пуш выполнены, текущая ветка — `dev`; слияние с `main` — по запросу пользователя.

## ВАЖНО!!!:

### MCP - сервера

- Не забывать использовать mcp инструменты.

### Согласование

- Все изменения согласовывать с пользователем.

### Язык

- Отвечать на русском языке.

### Правила

- Соблюдать `.ycarules`, особенно `[Z2]`, `[Z5]`, `[U1]`, `[U2]`, `[G2]`, `[K1]/[K2]`, `[S7]`, `[E3]`.

### Неоднозначность

- При неоднозначности — переспрашивать.

### Завершение

- После завершения — запрос на коммит и пуш.