# Задание для SourceCraft `plans/promt10.md`

`plans/promt10.md`

**!!!ВАЖНО-.ycarules имеет высший приоритет!!!**

## Цель

Следующая версия проекта SysW — **v1.0.7**: завершение функционала глубокой подстановки модельных кодов (автоподбор входящих работ/запчастей в модельные) и стабилизация тестового покрытия после миграции данных на SQLite.

## Список задач


1. СОГЛАСОВАНИЕ И АКТИВАЦИЯ ПОДСТАНОВКИ МОДЕЛЬНЫХ КОДОВ:
   - Флаг ApplyMatLibSubstitution добавлен и выключен по умолчанию ([`Mod_Constants.bas`](src/modules/Mod_Constants.bas)).
   - Требуется: согласовать бизнес-правило (что приоритетнее — артикул или наименование, поведение при нескольких совпадениях из GetMatLibEntries, куда писать результат — колонки O15/AB28 уже выбраны), активировать флаг, обновить [`docs/table.md`](docs/table.md).
   - Учесть правило [Z6] (маппинг полей меняется только по письменному указанию Начальника мира).

2. АВТОМАТИЗАЦИЯ СБОРКИ ПРОЕКТА (единый конвейер):
   - Предложить и согласовать единый конвейер сборки: build_templates.py → migrate_models_to_sqlite.py → impVBA.py → run_tests.py.
   - Добавить контроль целостности БД после сборки (PRAGMA integrity_check, контрольные количества works/parts_catalog), резервное копирование перед пересборкой.

3. ДОКУМЕНТАЦИЯ, ВЕРСИЯ, ЧИСТОТА, GIT:
   - Обновить docs/table.md, docs/ARCHITECTURE.md, docs/DEVELOPER.md, docs/CHANGELOG.md ([U2], [G2]).
   - Версия 1.0.7 (update_version.py/config.py).
   - Чистка проекта от мусора ([S7]), конфликтные ветки, коммит/пуш, слияние dev→main.

## Итоговая последовательность

        flowchart TD
                A[Этап 0 Пререквизиты] --> B[Этап 1 Архивация планов]
                B --> C[Этап 2 Объединенный план]
                C --> D[Этап 3 Обновление документации]
                D --> E[Этап 4 Решение по версии]
                E --> F[Этап 5 Анализ и итоговый план]
                F --> G[Этап 6 Согласование с юзером]
                G --> H[Этап 7 Реализация]
                H --> I[Этап 8 Верификация]
                I --> J[Этап 9 Чистота проекта]
                J --> K[Этап 10 Коммит и пуш]
                K --> L[Этап 11 Слияние dev в main]

## Примечания

- все изменения согласовывать перед реализацией (создавать план в `plans/` или устный запрос у юзера)
- отвечать — только на русском языке
- комментарии в коде — на русском языке
- строго соблюдать правила `.ycarules` (особенно [Z6], [U1], [U2], [G2], [K1]/[K2], [S7], [E3])
- при неоднозначности — переспрашивать, а не делать предположения
- после завершения задачи проверка чистоты проекта от мусора
- после завершения всех задач - запрос у юзера и выполнение коммита и пуша