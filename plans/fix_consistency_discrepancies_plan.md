# План устранения расхождений системы с dev-веткой

> **Статус:** черновик — документ для согласования (правки в код/конфиги НЕ вносятся до утверждения).
> **Дата:** 2026-08-21
> **Приоритет правил:** `.ycarules` — [U1], [Z5], [E3], [G3], [S7], [K1]/[K2].
> **Целевое состояние:** все активные конфиги и документация не содержат абсолютного пути `L:\PROject\SysW`, ссылки ведут на реально существующие файлы, CI не падает.

---

## 0. Общие принципы исполнения

- Все ответы, комментарии и документы — только на русском ([G3]).
- Правки критических файлов (`README.md`, `.ycarules`, конфиги) — только после согласования плана ([E3], [U1]).
- Git-операции — через MCP Git Tools ([G10]); файловые операции — через MCP File System ([G11]), где уместно.
- Изменения VBA (`.bas` / `.cls`) — только через скрипты `impVBA.py` / `export_vba.py` ([S4], двухфазная кодировка [K1]/[K2]). **В рамках данного плана правки VBA-кода не предполагаются** — речь только о конфигах, документации и CI.
- Перед реализацией — обязательное согласование плана с пользователем (см. раздел 7).

---

## 1. Приоритет 1 — Работоспособность инструментов (критические)

### 1.1 `.codeassistant/mcp.json` — абсолютные пути серверов

**Файл:** [`.codeassistant/mcp.json`](.codeassistant/mcp.json)

**Расхождение:** пути серверов `git` (строка 13) и `filesystem` (строка 22) заданы **абсолютно** `L:/PROject/SysW`. В эталонном [`.sourcecraft`](.sourcecraft:9) те же серверы заданы **относительно** через `${workspaceFolder}`.

**Цель:** привести `mcp.json` к относительному виду или исключить из репозитория.

**Открытый вопрос (решение НЕ принимается без согласования):** `.codeassistant/mcp.json` — конфиг для Roo/Cline. Необходимо подтвердить, поддерживает ли выбранный инструмент подстановку переменной `${workspaceFolder}` в `command`/`args`.

**Варианты (выбрать один после проверки):**
- **Вариант A — `${workspaceFolder}` (предпочтительный, если формат поддерживает):**
  - `"L:/PROject/SysW"` → `"${workspaceFolder}"` для обоих серверов (по образцу `.sourcecraft`).
  - Плюсы: относительность, переносимость между машинами.
  - Минусы: требует, чтобы клиент (Roo/Cline) разворачивал переменную. Проверить в документации/тесте.
- **Вариант B — не коммитить mcp.json:**
  - Добавить `.codeassistant/mcp.json` в `.gitignore`.
  - Локально генерировать файл при первом запуске (шаблон + плейсхолдер пути), документировать процедуру в `docs/sourcecraft-guide.md`.
  - Плюсы: исключает попадание абсолютных путей в репозиторий полностью.
  - Минусы: требует ручной шаг генерации на каждой машине.
- **Вариант C — оставить как есть + документация:** зафиксировать в `docs/sourcecraft-guide.md`, что это локальный путь текущей машины и его нужно править локально; **не рекомендуется**, т.к. не устраняет корень проблемы.

**Проверка после реализации:** `mcp.json` не содержит `L:/PROject/SysW`; валидный JSON; серверы поднимаются.

---

### 1.2 `.vscode/settings.json` — активатор venv в профиле `SourceCraft`

**Файл:** [`.vscode/settings.json:29`](.vscode/settings.json:29)

**Расхождение:** в профиле терминала `SourceCraft` активация venv задана **абсолютно** `". \"L:\\PROject\\SysW\\.venv\\Scripts\\Activate.ps1\""`. При этом строка 18 (`python.defaultInterpreterPath`) уже использует `${workspaceFolder}/.venv/Scripts/python.exe`.

**Цель:** привести строку 29 к `${workspaceFolder}`-относительному виду, синхронизировав со строкой 18.

**Действие:**
- Заменить значение в `args` профиля `SourceCraft`:
  - было: `". \"L:\\PROject\\SysW\\.venv\\Scripts\\Activate.ps1\""`
  - стало: `". \"${workspaceFolder}\\.venv\\Scripts\\Activate.ps1\""`
- Убедиться, что профиль по-прежнему использует PowerShell 7 (`pwsh.exe`) согласно [S9].

**Проверка после реализации:** в `settings.json` нет `L:\PROject\SysW`; профиль `SourceCraft` корректно активирует venv.

---

### 1.3 (дополнительно выявлено) `.github/workflows/vba-check.yml` — список проверяемых файлов не соответствует фактическому набору

**Файл:** [`.github/workflows/vba-check.yml:29`](.github/workflows/vba-check.yml:29) (и строка 41)

**Расхождение:** workflow перечисляет файлы, которых нет в репозитории:
- модули `Mod_MainButtons.bas` (нет; есть `Mod_ButtonDispatcher.bas`);
- классы листов `Sheet1_main.cls`, `Sheet_work.cls`, `Sheet_z4.cls` (в `src/sheets/` фактически только `Лист2_main.cls`).

**Следствие:** job `check-vba` будет падать (файлы помечаются `[FAIL] ... MISSING!`), что блокирует CI на ветках `main`/`dev`.

**Цель:** привести список проверяемых файлов в соответствие с фактическим составом `src/`.

**Действие (после согласования):**
- Сверить актуальный состав `src/modules/*.bas` и `src/sheets/*.cls`.
- Обновить оба списка (существование + UTF-8) в `vba-check.yml`.
- Учесть кириллическое имя `Лист2_main.cls` — решение о переименовании класса листа см. в пункте 6.2 (требует уточнения); если класс остаётся кириллическим, CI должен проверять реальное имя.

**Проверка после реализации:** `python`-этапы `vba-check.yml` проходят локально на текущем наборе файлов.

> Примечание: этот пункт связан с вопросом 6.2 (`Sheet_work.cls`). Финальный список в CI зависит от решения о составе `src/sheets/`.

---

## 2. Приоритет 2 — Документация

### 2.1 `README.md` — структура проекта и CI

**Файл:** [`README.md`](README.md)

**Расхождения:**
1. Строка 68: показан `CHANGELOG.md` в корне — фактически файл лежит в [`docs/CHANGELOG.md`](docs/CHANGELOG.md). В строке 138 уже стоит корректная ссылка на `docs/CHANGELOG.md`.
2. Строки 36, 79: корневой путь указан как `L:\PROject\SysW`.
3. Строки 63–64: в схеме отражён только `vba-check.yml`; не отражён `docs-check.yml`; нет описания самих workflow.

**Цель:** актуализировать схему структуры и раздел CI.

**Действия:**
- Строка 68: `CHANGELOG.md` → `docs/CHANGELOG.md`.
- Строки 36, 79: заменить корневой путь на актуальный. **Открытый вопрос для согласования:**
  - Вариант A: локальный путь `f:\MiTyA\PROject\SysW`.
  - Вариант B (предпочтительный для README): `<url-репозитория>` / плейсхолдер без привязки к машине (в схеме — псевдоним, например `SysW\`; в «Быстром старте» — `cd SysW` после `git clone`).
- В блоке `.github/workflows/` схемы: добавить `docs-check.yml` и `vba-check.yml`.
- Добавить краткий раздел **«CI/CD»**: перечислить оба workflow (назначение, триггеры: push/PR на `main`/`dev`):
  - `vba-check.yml` — проверка наличия и UTF-8 VBA-файлов, базовый синтаксис;
  - `docs-check.yml` — проверка консистентности документации (`python scripts/check_docs.py --check`).
- Согласовать внесение правок в критический файл `README.md` ([E3]).

**Проверка после реализации:** ссылки в README ведут на существующие файлы; схема отражает реальную структуру; раздел CI описывает оба workflow.

---

### 2.2 `.sourcecraft:49` — ссылка на несуществующий файл

**Файл:** [`.sourcecraft:49`](.sourcecraft:49)

**Расхождение:** в `rules.include` указан `docs/ARCHITECTURE_SQLITE.md`, которого нет — существует только [`docs/ARCHITECTURE.md`](docs/ARCHITECTURE.md).

**Открытый вопрос для согласования (решение НЕ принимается без пользователя):**
- **Вариант A (рекомендуется):** исправить ссылку на реальный `docs/ARCHITECTURE.md`.
- **Вариант B:** создать отдельный файл `docs/ARCHITECTURE_SQLITE.md` (выделить SQLite-раздел из `ARCHITECTURE.md`) и сохранить ссылку.

**Проверка после реализации:** все значения `include`/`exclude`/`critical` в `.sourcecraft` ссылаются на существующие пути.

---

## 3. Приоритет 3 — Требует уточнения (решения пользователя)

### 3.1 Остаточные упоминания Syncthing в планах

**Файлы:** [`plans/integration_1.0.1.md`](plans/integration_1.0.1.md), архивные `plans/_archive/*` (`integration_1.0.2.md`, `sync_fix_plan.md` и др.)

**Контекст:** согласно [`plans/_archive/integration_1.0.2.md`](plans/_archive/integration_1.0.2.md:8), Syncthing был полностью удалён из проекта (`.stignore`, `scripts/sync_fix.ps1`). При этом в `integration_1.0.1.md` (строки 99, 240, 244) и в архивных планах остались упоминания.

**Вопрос к пользователю:** эти упоминания являются **историей** (архивные документы фиксируют факт удаления) или **требуют правок**?
- **Вариант A (рекомендуется):** оставить как есть — `plans/_archive/*` и `integration_1.0.1.md` фиксируют исторические решения; правки в архив не вносим (правило [Z4] — не менять существующее без запроса).

### 3.2 Отсутствие `src/sheets/Sheet_work.cls`

**Файлы:** `src/sheets/` (фактически только `Лист2_main.cls`), `work.xlsm`, `.github/workflows/vba-check.yml`.

**Контекст:** в `src/sheets/` нет `Sheet_work.cls`, но на него ссылается `vba-check.yml` (см. пункт 1.3). Также планировочная документация (README: «3 .cls листа») расходится с фактическим одним классом листа.

**Вопрос к пользователю:** подтвердить, что это **намеренная чистка дублей** (в пользу `Лист2_main.cls`), а не потеря модуля, на который ссылается `work.xlsm`.
- **Вариант A (предположительно намеренная чистка):** обновить CI и документацию под фактический состав (см. 1.3), не восстанавливая `Sheet_work.cls`.
- **Вариант B:** модуль нужен — восстановить `Sheet_work.cls` (и/или другие классы листов) из `work.xlsm`/истории и обновить CI.

> Решение по пункту 3.2 напрямую влияет на финальное содержимое `vba-check.yml` (пункт 1.3).

---

## 4. Этап согласования (обязательный, перед реализацией)

Правила проекта ([U1], [E3], [Z4], [Z5]) требуют утверждения плана пользователем до внесения любых изменений.

1. Показать пользователю настоящий план.
2. Получить решения по **открытым вопросам**:
   - `.codeassistant/mcp.json`: вариант A/B/C (проверить поддержку `${workspaceFolder}`);
   - README корневой путь: вариант A/B;
   - `.sourcecraft`: вариант A/B по `ARCHITECTURE_SQLITE.md`;
   - Syncthing: вариант A/B/C;
   - `Sheet_work.cls` / `src/sheets`: вариант A/B.
3. После утверждения — перейти к реализации (роль `Code`).

---

## 5. Этап верификации после реализации

После внесения правок выполнить проверки:

- [ ] Во всех **активных** конфигах и документации отсутствует `L:\PROject\SysW` (поиск по репозиторию, кроме истории git и `_backup`).
- [ ] JSON-файлы валидны: `.codeassistant/mcp.json`, `.sourcecraft`, `.vscode/settings.json` (парсер JSON).
- [ ] Ссылки в `README.md` и `.sourcecraft` ведут на существующие файлы (`docs/CHANGELOG.md`, `docs/ARCHITECTURE.md`).
- [ ] `vba-check.yml` проверяет только существующие файлы; локальный прогон соответствующих шагов проходит.
- [ ] Профиль терминала `SourceCraft` активирует venv через `${workspaceFolder}`.

---

## 6. Этап проверки чистоты проекта

Правило [S7]:

- [ ] Нет временных файлов: `__pycache__`, `*.pyc`, `~$*.xls*`, `*.tmp`, `*.log` в нецелевых местах.
- [ ] Нет конфликтных копий `*.sync-conflict-*`.
- [ ] Нет `Thumbs.db`, `Desktop.ini`, `.DS_Store`.
- [ ] `workOt/` и `_backup/` не закоммичены (исключены).
- [ ] Удалить любые временные файлы, созданные ассистентом в ходе реализации.

---

## 7. Этап финального коммита и пуша

1. Выполнить коммит изменений через MCP Git Tools ([G10]) с осмысленным сообщением в формате Conventional Commits (см. `docs/git-workflow.md`), например:
   - `fix(config): make mcp.json and venv activation path relative`
   - `docs(readme): fix CHANGELOG path and add CI section`
   - `ci(vba-check): sync checked files with actual src structure`
2. При необходимости разбить на несколько коммитов по логическим блокам (конфиги / README / CI).
3. Выполнить пуш в соответствующую ветку.
4. Обновить `docs/CHANGELOG.md` при наличии функциональных изменений ([U2]).

---

## 8. Итоговый чек-лист

- [ ] План согласован с пользователем.
- [ ] Приоритет 1 (инструменты): mcp.json, settings.json, vba-check.yml приведены к рабочему состоянию.
- [ ] Приоритет 2 (документация): README и .sourcecraft актуализированы.
- [ ] Приоритет 3 (уточнения): получены и учтены решения пользователя.
- [ ] Верификация пройдена (раздел 5).
- [ ] Чистота проекта подтверждена (раздел 6).
- [ ] Коммит и пуш выполнены (раздел 7).