ДОКУМЕНТАЦИЯ ЗАДАНИЯ:

# Задание для SourceCraft `plans\_promt`

`plans\_promt`

**!!!ВАЖНО-.ycarules имеет высший приоритет!!!**

## Список задач

1. интегрировать в систему файлы шаблоны  L:\PROject\SysW\base\templates:

   1. work0.xlsm - файл шаблон work должен быть пустой, без кодов
   2. model0.xlsm -файл шаблон для создания модельного файла пустой, без кодов
   3. work.xlsm - файл шаблон work, с последним обновлением кодовой базы
   4. model0.xlsm - файл шаблон для создания модельного файла с последним обновлением кодовой базы
   5. report0.xlsx - файл шаблон файла-отчета пустой, без кодов


2. разработать и интегрировать следущие изменения в систему:

    - листы: main, все листы с запчастями, все листы с работами, а так же другие листы имеющие заголовки, должны быть защищены от изменений форматирования в 3 верхних строках (2 под кнопки + хаголовки), а лист main еще и первые 2 столбца


3. синхронизировать версии коммитов с версиозностью системы

4. установить версию системы 1.0.4

5. провести анализ задач ask

6. на основе анализа создать план интеграции architector
7. реализация плана code
8. проверка системы на: code

    - мусор

    - соответствие документации

## Примечания

- все изменения согласовывать перед реализацией (создавать план в `plans/` или устный запрос у юзера)
- отвечать — только на русском языке
- комментарии в коде — на русском языке
- строго соблюдать правила `.ycarules`
- при неоднозначности — переспрашивать, а не делать предположения
- пользоваться mcp
- использовать роли актуальные для задачи/подзадачи
- после завершения задачи проверка чистоты проекта от мусора
- после завершения всех задач - запрос у юзера и выполнение коммита и пуша
