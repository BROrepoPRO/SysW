1.1.3

- Устранить ограничение scripts/check_docs.py (regex распознаёт только версии X.Y.Z, а не X.Y.Z.W — 6 ложных флагов, не регрессия).
- По K-02 — оптимизация производительности AutoMatchWorks (кэш/индекс модельных артикулов);
- по K-01 — проверка логики B4 в Mod_Import.ImportFromB2_UI.
- Следующий алгоритм для бизнес-теста: сверка глубокой подстановки O/AB против matlib_entries;
- после генерации z4.xlsx (запросить у пользователя чтобы он сформировал z4.xlsx) (R-Z4GEN) — fallback-поиск ЗЧ (ReadGlobalPartByKey).
- Git push dev и слияние dev→main при готовности рабочей схемы.

