# Задание для SourceCraft `plans/promt1.1.2.md`

**!!!ВАЖНО: `.ycarules` имеет высший приоритет!!!**

## Цель

### Версия

Версия проекта SysW — **1.1.2.1** (формат X.Y.Z.W, шаг внутри промта — `W=1`).

Направления промта:

1. **Финализация и документирование итерации 1.1.2** — зафиксировать выполненную
   работу подзадачи C: верификацию шапки заказа после импорта
   (`scripts/run_p1_business_test.py`, функция `snapshot_order_header`, сводка `[ШАПКА]`),
   интеграционное тестирование `ImportFromB2_UI` (группа `RunImportB2IntegrationTests`,
   TC-65..TC-68) и результаты прогонов на реальных данных.
2. **Синхронизировать версию 1.1.2.x** по всем источникам версии проекта.
3. **Задокументировать НАБЛЮДЕНИЯ** бизнес-теста как известные проблемы/техдолг
   (не исправлять код в этой итерации).

## Предшествующий статус (1.1.1 → 1.1.2)

Источник статуса — [`docs/CHANGELOG.md`](docs/CHANGELOG.md) §v1.1.1.1 и `docs/ROADMAP.md` §11.

### Выполнено в 1.1.1 (текущее состояние до 1.1.2)

- **Фикс зависания `AutoMatchWorks`** — защитные параметры вокруг `Workbooks.Open`
  в `OpenModelGroupFile` ([`Mod_ModelDB.bas`](src/modules/Mod_ModelDB.bas));
  бизнес-прогон без зависания.
- **Дефект кириллицы R-16** — `_read_text_robust` в `run_tests.py` построчно декодирует
  UTF-8 → cp1251; отчёты JSON/HTML корректны.
- **SQLite-таймаут** — `ConnectionTimeout`/`CommandTimeout = 15` в
  [`Mod_SQLiteDB.cls`](src/classes/Mod_SQLiteDB.cls).
- **CLI-интерфейс `run_tests.py`** — `--module`, `--verbose`, `--output`, отчёты JSON/HTML.

### Что переносится из 1.1.1 (незавершённое)

- T02, T04, T14, T15 — закрытие «РАБОЧЕЙ СХЕМЫ» и синхронизация todo↔CHANGELOG↔commits.
- Отложенные задачи Фазы 1.2 / глобальные цели (§11 ROADMAP) — вне scope данной итерации.

## Выполненные задачи итерации 1.1.2 (подзадача A/B — уже реализованы)

> Код и скрипты этих задач **уже готовы** и в scope данной итерации **не изменяются**
> (только фиксируются в документации/плане).

### Подзадача A — Верификация шапки заказа после импорта

- В [`scripts/run_p1_business_test.py`](scripts/run_p1_business_test.py) добавлена
  верификация шапки заказа после импорта: диапазон `B4/B5:B17` листа `main`,
  функция `snapshot_order_header`, итоговая сводка `[ШАПКА]`.
- Результат прогона на реальных данных: **1 работа + 1 ЗЧ**; шапка `B5:B17` заполнена
  (ключевые поля 5/5: B5 модель, B6 ГРЗ, B12 №ЗН, B13 цена н/ч, B14 группа).
- Отчёт: `logs/p1_test_report.log`.

### Подзадача B — Интеграционное тестирование ImportFromB2_UI (R-IT)

- Отложенная задача **R-IT** решена: добавлена группа `RunImportB2IntegrationTests`
  (TC-65..TC-68) в [`Mod_FullTestRunner.bas`](src/modules/Mod_FullTestRunner.bas).
- Прогон `scripts/run_tests.py`: **Total=69 / Passed=60 / Failed=0 / Skipped=9**.
- R-IT отмечена выполненной в `docs/ROADMAP.md`; обновлены `docs/DEVELOPER.md` и
  `docs/table.md`.

## НАБЛЮДЕНИЯ из бизнес-теста (известные проблемы / техдолг)

> Документируются как известные проблемы. **В этой итерации не исправляются.**

1. **Номер заказа B4 пуст после импорта** — до импорта был `100`, шапка `B5:B17`
   заполняется корректно, но сама ячейка `B4` очищается в процессе импорта.
   Рекомендация: проверить логику очистки/переноса B4 в `Mod_Import.ImportFromB2_UI`.
2. **`AutoMatchWorks` медленный** — фикс зависания сработал (без зависания), но время
   выполнения очень большое (~2 ч 46 мин на 1 работе). Кандидат на оптимизацию
   производительности (кэширование/поиск по индексу модельных артикулов).
3. **`AutoMatchWorks`/`AutoMatchParts` не подобрали артикулы** — пусто вместо
   «НЕ НАЙДЕНО», при том что модельные артикулы O/AB проставлены. Рекомендация:
   проверить отображение состояния «не найдено» в результатах автоподбора.

## Задачи (скоуп строго по пунктам)

### Задача 1 — Создать план-файл `plans/promt1.1.2.md`

Настоящий файл (по образцу `plans/promt1.1.1.md`): цель, выполненные задачи, результаты
тестов, наблюдения, переносимые пункты, следующий алгоритм бизнес-теста.

### Задача 2 — Обновить `docs/CHANGELOG.md` (раздел v1.1.2)

- Добавить раздел **v1.1.2.1** в формате Keep a Changelog со всеми изменениями и
  результатами тестов из контекста (пп.1–3): верификация шапки, R-IT/TC-65..68,
  результаты прогона Total=69/Passed=60/Failed=0/Skipped=9.
- Включить блок «Известные проблемы / Наблюдения» с тремя пунктами выше.

### Задача 3 — Синхронизировать версию 1.1.2.1 по всем источникам

Использовать `scripts/update_version.py 1.1.2.1` (X.Y.Z.W); при необходимости — ручная
согласованная правка:

- `docs/CHANGELOG.md` (раздел v1.1.2.1),
- `docs/ROADMAP.md` (блок «Версия системы», устаревший заголовок/статус),
- `README.md` (заголовок `**Версия:** v...`),
- `scripts/config.py` (`APP_VERSION`),
- `src/modules/Mod_Constants.bas` (`APP_VERSION`),
- `scripts/config.ps1` (`$Script:AppVersion`),
- `docs/DEVELOPER.md`, `docs/ARCHITECTURE.md` (заголовки версии).

### Задача 4 — Задокументировать наблюдения как известные проблемы/техдолг

- В `docs/CHANGELOG.md` (раздел v1.1.2.1) — блок «Известные проблемы / Наблюдения».
- В `docs/ROADMAP.md` — подраздел известных проблем/техдолгов с рекомендациями
  (оптимизация `AutoMatchWorks`, проверка очистки B4, корректность «НЕ НАЙДЕНО»).
- Код **не исправлять**.

### Задача 5 — Контрольные проверки

- `python scripts/check_docs.py --check` (при необходимости `--fix`).
- `python scripts/check_vba_syntax.py`.
- Убедиться в отсутствии регрессий в документации/синтаксисе.

### Задача 6 — Git-коммит

- Локальный коммит в ветке `dev` с понятным сообщением (Conventional Commits,
  область/версия).
- **НЕ** выполнять `git push`, **НЕ** сливать в `main` без явного запроса.

## Следующий алгоритм для бизнес-теста (переносится)

1. После устранения техдолга «B4 пуст после импорта» — перепроверить, что номер заказа
   B4 сохраняется после импорта (в дополнение к шапке B5:B17).
2. Оптимизировать `AutoMatchWorks` (производительность) и повторить замер времени на
   1 работе.
3. Проверить отображение «НЕ НАЙДЕНО» при отсутствии подобранных артикулов.
4. Повторный полный прогон `run_tests.py` (контроль Total=69/Passed) и
   `run_p1_business_test.py` (контроль сводки `[ШАПКА]`) на актуальных данных.

## РАБОЧАЯ СХЕМА

### 1. Анализ задачи (роль: **Ask**)

- анализ для планирования; зарегистрировать задачи промта в todo.

### 2. Планирование (роль: **Architect**)

- на основе анализа Ask спроектировать план обновлений по текущему промту.

### 3. Исполнение (роль: **Code**)

- документация, синхронизация версии, контрольные проверки, коммит.

### 4. Проверка (роль: **Debug**)

- контрольные проверки `check_docs.py`, `check_vba_syntax.py`.

### 5. Координация (роль: **Orchestrator**)

- контролировать работу с todo.

### 6. Обновление документации

Обновить: `docs/CHANGELOG.md`, `docs/ROADMAP.md` (+ по синхронизации версии —
`README.md`, `docs/DEVELOPER.md`, `docs/ARCHITECTURE.md`).

### 7. Очистка системы

- Убедиться, что временные файлы не остались; проверить актуальность упоминаний версии.

### 8. Коммит

- Локальный коммит в ветку `dev`. Пуш/слияние `dev → main` — только по явному запросу.

## Scope (границы)

### Входит в промт

- План `plans/promt1.1.2.md`, `docs/CHANGELOG.md` (раздел v1.1.2.1), `docs/ROADMAP.md`,
  синхронизация версии по всем источникам, контрольные проверки, локальный коммит в `dev`.

### Вне scope (не выполняем)

- **Код**: `src/**` (в т.ч. `Mod_FullTestRunner.bas`), `scripts/run_p1_business_test.py` —
  уже готовы; не меняются.
- **Исправление наблюдений** (B4, AutoMatchWorks, «НЕ НАЙДЕНО») — только документация.
- **Запуск долгого бизнес-теста** заново (~3 ч) — используются полученные результаты.
- **git push** и **слияние в `main`** без явного запроса.

## Ограничения

- Изменяются только документация, планы и файлы версии (правило `[Z5]`/`[U2]`).
- Кодировка: Python-скрипты — UTF-8; VBA-файлы на диске — UTF-8 (без BOM), в Excel —
  CP1251 (правило K1-K3).
- Не выдумывать данные сверх того, что есть в реальном коде/документации; ссылаться на
  фактические модули и скрипты.
- Версия проекта — единая 1.1.2.1 по всем источникам (правило `[U7]`).

## Критерии приёмки

- План `plans/promt1.1.2.md` создан.
- `docs/CHANGELOG.md` содержит раздел v1.1.2.1 с результатами тестов и известными проблемами.
- Версия 1.1.2.1 синхронизирована во всех источниках; `docs/ROADMAP.md` заголовок актуален.
- Наблюдения задокументированы как известные проблемы/техдолг; код не изменён.
- Контрольные проверки без регрессий.
- Локальный git-коммит в `dev`; push/merge в `main` не выполнялись.

## ВАЖНО!!!

### MCP - сервера

- Не забывать использовать mcp инструменты.

### Согласование

- Все изменения согласовывать с пользователем.

### Язык

- Отвечать на русском языке.

### Правила

- Соблюдать `.ycarules`, особенно `[Z2]`, `[Z5]`, `[U1]`, `[U2]`, `[G2]`, `[K1]/[K2]`,
  `[S7]`, `[E3]`, `[U4]`, `[U5]`, `[U7]`.

### Неоднозначность

- При неоднозначности — переспрашивать.

### Завершение

- После завершения — запрос на коммит (пуш/слияние — только по явному запросу).