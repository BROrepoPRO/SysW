## Полный реестр макросов с рекомендациями

### 1.1 Кнопки листа main (обработчики `Mod_ButtonDispatcher`)

| Макрос                        | Подробное описание                                                                                                                                                                                                                                                                                                             | Кнопка                      | Рекомендация                                                                                                                         | Решение юзера                                                                       |
| ----------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------- |
| `Btn_main_Clear_Click`            | Вызывает`Mod_SheetOps.ClearMainSheet_UI`. Очищает данные на листе `main` с ячейки B4 до последнего столбца ZZ, не затрагивая строки 1–3 (технические и заголовки). При `silent=False` запрашивает подтверждение. | «Очистить»              | Оставить, проверить, что строки 1–3 не очищаются.                                                          | +                                                                                               |
| `Btn_main_Import_Click`           | Читает ГРЗ из ячейки**B6** листа `main`, вызывает `Mod_Import.ImportSheet` для импорта листа из `report.xlsx` по этому ГРЗ. Если ГРЗ пуст – выводит сообщение.                                                                                 | «Импорт»                  | Оставить, но сверить актуальность ячейки B6 (возможно, должен быть B7 или другая). | удалить                                                                                  |
| `Btn_main_FillHeader_Click`       | Читает номер заказа из ячейки**B4**, вызывает `Mod_OrderHeader.FillHeaderFromOrder`. Заполняет шапку B5:B17 данными из листов `spisok` и `models`. Показывает результат.                                                                          | «Заполнить шапку» | Оставить, проверить соответствие маппинга v1.0.9.                                                           | +                                                                                               |
| `Btn_main_ClearHeader_Click`      | Вызывает`Mod_SheetOps.ClearHeader_UI`. Очищает только шапку – диапазон **B5:B17**, не трогая B4 (номер заказа).                                                                                                                                                                   | «Очистить шапку»   | Оставить.                                                                                                                                | +                                                                                               |
| `Btn_main_ImportByInput_Click`    | Запрашивает ГРЗ через`InputBox`, затем вызывает `Mod_Import.ImportSheet` для импорта из `report.xlsx`.                                                                                                                                                                                        | «Импорт по ГРЗ»      | Оставить как вспомогательный.                                                                                          | удалить                                                                                  |
| `Btn_main_RunTests_Click`         | Вызывает`Mod_FullTestRunner.RunAllTests`. Запускает все автотесты, выводит сводку (всего/пройдено/провалено/пропущено).                                                                                                                                              | «Тесты»                    | Оставить.                                                                                                                                | актуализировать после обновлений                                  |
| `Btn_main_WriteLog_Click`         | Открывает`InputBox` для ввода сообщения и записывает его в лог через `Mod_Logger.WriteLog`.                                                                                                                                                                                                | «Лог»                        | Оставить.                                                                                                                                | рассказать подробнее                                                         |
| `Btn_main_RenameSheets_Click`     | Вызывает`Mod_Import.RenameSheets_UI`. Открывает `report.xlsx` и переименовывает видимые листы по числовой части ГРЗ, найденной в строке с «автомобиль».                                                                                         | «Переименовать»    | Оставить, возможно, использовать редко.                                                                         | удалить                                                                                  |
| `Btn_main_ImportDataToMain_Click` | Берёт активный лист как источник и вызывает`Mod_Import.ImportDataToMain` для переноса данных на `main`.                                                                                                                                                                             | «Перенести в main»    | Оставить.                                                                                                                                | рассказать подробнее                                                         |
| `Btn_main_FindOrder_Click`        | Вызывает`Mod_OrderHeader.FindOrder_UI`. Запрашивает номер заказа, ищет в `spisok`, выводит найденные данные.                                                                                                                                                                       | «Найти заказ»         | Оставить.                                                                                                                                | рассказать подробнее                                                         |
| `Btn_main_ShowWorkbookPath_Click` | Показывает путь к текущей книге (`ThisWorkbook.Path`).                                                                                                                                                                                                                                                             | «Путь к книге»        | Оставить, но можно скрыть с интерфейса.                                                                          | удалить, если не критично                                                  |
| `Btn_main_ShowCurrentUser_Click`  | Показывает имя пользователя Windows (`Environ("USERNAME")`).                                                                                                                                                                                                                                                         | «Пользователь»      | Оставить, можно скрыть.                                                                                                       | удалить, если не критично                                                  |
| `Btn_main_CheckFileExists_Click`  | Запрашивает путь к файлу, проверяет существование через`FileExists`, выводит результат.                                                                                                                                                                                       | «Проверить файл»   | Оставить.                                                                                                                                | +                                                                                               |
| `Btn_main_ImportVH_Click`         | «Импорт ВХ»: читает номер заказа из B4, ищет лист`{B4}M` в книге. Если лист есть – сразу переносит данные; если нет – сообщение.                                                                                                                 | «Импорт ВХ»             | Критичный макрос, обязательно проверить на реальных данных.                                   | редактировать                                                                      |
| `Btn_main_ImportFromSheetM_Click` | Импорт с готового листа`{B4}M` без обращения к `report.xlsx`. Просто берёт лист с таким именем и переносит данные на `main`.                                                                                                                                | «Импорт с листа M»  | Оставить.                                                                                                                                | сравнить с`Btn_main_ImportVH_Click`, удалить, если не критично |

### 1.2 Автоподбор / ручной подбор

| Макрос                      | Подробное описание                                                                                                                                                                                                                                                                                                          | Кнопка        | Рекомендация                                                                              | Решение юзера            |
| --------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------- | ----------------------------------------------------------------------------------------------------- | ------------------------------------ |
| `Btn_main_AutoMatchWorks_Click` | Вызывает`Mod_AutoMatch.AutoMatchWorks`. Читает импортированные работы (L:N), подбирает модельные работы из файла группы, записывает результат в столбцы E:I и формулу в J.                                                     | «АВТО РАБ» | Проверить на реальных данных, так как сложный алгоритм. | +                                    |
| `Btn_main_AutoMatchParts_Click` | Вызывает`Mod_AutoMatch.AutoMatchParts`. Аналогично для запчастей: читает X:AA, подбирает, пишет в Q:U и формулу V.                                                                                                                                                              | «АВТО ЗЧ»   | Проверить на реальных данных.                                                | +                                    |
| `Btn_main_PickWork_Click`       | Вызывает`Mod_PickWork.PickWork_UI`. Читает группу из B14, открывает файл `base/models/{Group}.xlsm`, активирует лист работ. Пользователь вручную ищет и копирует работы в диапазон E4:H. Показывает инструкцию. | «РУЧ РАБ»   | Оставить, улучшить инструкцию.                                              | + добавить «РУЧ ЗЧ» |

### 1.3 Поиск листов UAZ и запчастей (`Mod_SheetButtons`) - ОШИБКА UAZ - это был как пример!!!

!!! значение`UAZ ` или другое импортируется из main!$B$14

ЗАДАЧИ:

- совместно с юзером разработать новую структуру книги model чтобы имена листов легко подходили под макросы в книгах с разными менами
- base\templates\work.xlsm  - импорт модулей делать сюда тоже, считать этот файл исходным (с импортированными модулями, но без данных)
- base\templates\work0.xlsm  - считать этот файл исходным (без данных, без модулей, но с исходным форматированием)
- `- Btn_UAZ_Article_Click` → `Btn_UAZ_SearchByArticle` - должны работать в книге с любым именем, UAZ из названия убрать
- настроить импорт модулей VBA в модельльные книги base\models\ аналогично импорту works.xlsm
- base\templates\model0.xlsm - считать этот файл исходным образцом (без данных, без модулей, но с исходным форматированием)
- base\templates\model.xlsm  - импорт модулей делать сюда тоже, считать этот файл исходным образцом (с импортированными модулями, но без данных)
- в случае поломки брать исходники отсюда: base\templates\

| Макрос                                                                                                                                      | Подробное описание                                                                                                                               | Кнопка                                 | Рекомендация                               | Решение юзера                                                   |
| ------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------- | ------------------------------------------------------ | --------------------------------------------------------------------------- |
| `Btn_UAZ_Article_Click` → `Btn_UAZ_SearchByArticle`                                                                                          | Поиск «содержит» по столбцу B (артикул) на листе UAZ. Использует AutoFilter.                                      | «Поиск по артикулу»         | Оставить.                                      | Переделать все макросы под новые условия |
| `Btn_UAZ_Name_Click` → `Btn_UAZ_SearchByName`                                                                                                | Поиск по столбцу C (наименование) на листе UAZ.                                                                                  | «Поиск по наименованию» | Оставить.                                      |                                                                             |
| `Btn_UAZ_Clear_Click` → `Btn_UAZ_ClearFilter`                                                                                                | Сброс фильтра на листе UAZ.                                                                                                                    | «Сброс»                               | Оставить.                                      |                                                                             |
| `Btn_Parts_Article_Click` → `Btn_Parts_SearchByArticle`                                                                                      | Поиск по столбцу B (артикул) на листах`z4` или `*z4`.                                                                         | «Поиск по артикулу»         | Оставить.                                      |                                                                             |
| `Btn_Parts_Name_Click` → `Btn_Parts_SearchByName`                                                                                            | Поиск по столбцу C (наименование) на листах запчастей.                                                                 | «Поиск по наименованию» | Оставить.                                      |                                                                             |
| `Btn_Parts_Clear_Click` → `Btn_Parts_ClearFilter`                                                                                            | Сброс фильтра на листе запчастей.                                                                                                     | «Сброс»                               | Оставить.                                      |                                                                             |
| *Приватные:* `ExecuteUAZSearch(searchColumn)`, `ExecutePartsSearch(searchColumn)`, `IsSearchableSheet(ws)`, `IsPartsSheet(ws)` | Вспомогательные функции для определения листа и выполнения фильтра по нужному столбцу. | —                                           | Не трогать без необходимости. |                                                                             |

